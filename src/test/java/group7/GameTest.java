package group7;

import group7.gameStates.gameStates;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;
// TODO: game class might need to be tested in app class test
public class GameTest {
    /* 
    Game game = new Game();


    @Test
    public void changeGameStatesTest() {
        assertEquals( gameStates.IN_MENU, game.getCurrentGameState() ); // default gameState is in_menu  

        game.changeGameStates( gameStates.IN_LEVEL );
        assertEquals( gameStates.IN_LEVEL, game.getCurrentGameState() ); // changed state of game

        //game.changeGameStates( gameStates.IN_LEVEL );
        //assertEquals( gameStates.IN_LEVEL, game.getCurrentGameState() ); // changed state of game
    }
    */
}